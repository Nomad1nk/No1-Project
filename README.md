# 🤖 Bedel AI Box (Receptionist)

Монгол хэлтэй, хиймэл оюун ухаант хүлээн авагч. Yeastar TG100 болон Mini PC ашиглан дуудлагад автоматаар хариулж, захиалга авна.

## 🚀 Суулгах заавар

### 1. Шаардлага
*   **Hardware**: Intel N100 Mini PC (эсвэл Laptop)
*   **OS**: Ubuntu 22.04+ (эсвэл Windows + WSL2)
*   **Gateway**: Yeastar TG100 (GSM Gateway)

### 2. Ажиллуулах (Docker)
```bash
# 1. Репозиторыг татах
git clone [REPO_URL]
cd NewPRO

# 2. Тохиргоо хийх (.env)
cp .env.example .env
# .env дотор API түлхүүрүүдээ хийнэ үү.

# 3. Ажиллуулах
docker compose up --build -d
```

### 3. Лиценз идэвхжүүлэх
Эхний удаа ашиглахад "Invalid License Key" гэсэн алдаа гарна.
1.  Лог дээрээс `Device MAC`-ийг харна уу.
2.  `src/license.py` ашиглан түлхүүр үүсгэнэ (Зөвхөн Админ).
3.  `.env` файл дотор `LICENSE_KEY=XXXX-XXXX...` гэж нэмнэ.

### 4. Удирдлагын самбар
Browser дээрээ `http://localhost:8000` хаягаар орж:
*   Дуудлагын түүх харах
*   Барааны үнэ өөрчлөх
*   AI-ийн зан авирыг (Prompt) өөрчлөх боломжтой.

## 📞 Yeastar холболт
`yeastar_config_guide.md` файлыг уншина уу.

## 📁 Файлын бүтэц
*   `src/main.py`: Үндсэн програм
*   `src/api/`: Вэб сайт (Dashboard)
*   `src/session.py`: Дуудлага удирдах
*   `setup_minipc.sh`: Mini PC суулгах скрипт
